project:
  name: relay
  version: "0.1.0"
  schemas:
    - common
    - aspice
  description: >
    Formally verified flight software engines for cFS replacement.
    16 engines, all Verus SMT/Z3 proven. Drop-in replacements for
    NASA cFS applications.

sources:
  - path: artifacts/sysreq
    format: generic-yaml
  - path: artifacts/swarch
    format: generic-yaml
  - path: artifacts/swreq
    format: generic-yaml
  - path: artifacts/swdd
    format: generic-yaml
  - path: artifacts/verification
    format: generic-yaml

docs:
  - docs

results: results
