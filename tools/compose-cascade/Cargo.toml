[package]
name = "compose-cascade"
version = "0.1.0"
edition = "2021"
publish = false
description = "Compose the published falcon stage components into a self-contained cascade"

# Standalone workspace: this is build tooling, not flight code, and it must not
# drag wasmparser into the workspace graph the flight crates resolve against.
[workspace]

[dependencies]
anyhow = "1"
wasmparser = "0.221"
