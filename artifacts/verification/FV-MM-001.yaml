artifacts:
  - id: FV-MM-001
    type: unit-verification
    title: "Formal verification for SWDD-MM-001"
    status: approved
    description: >
      Verus SMT/Z3 formal verification of relay-mm engine properties
      MM-P01 through MM-P04. All 4 properties proven. Run via:
      bazel test //:relay_mm_verus_test
    tags: [verification, formal, verus, memory-manager]
    fields:
      method: formal-verification
      steps:
        # Cross-checked against BUILD.bazel by the gate
        # (enforced-by-verus-gate); EXECUTED by verus.yml.
        # The old citation //:verus_relay_mm never existed (#364).
        - run: "bazel test //:relay_mm_verus_test"
    links:
      - type: verifies
        target: SWDD-MM-001
