artifacts:
  - id: FV-SC-001
    type: unit-verification
    title: "Formal verification for SWDD-SC-001"
    status: approved
    description: >
      Verus SMT/Z3 formal verification of relay-sc engine properties
      SC-P01 through SC-P05. All 5 properties proven. Run via:
      bazel test //:relay_sc_verus_test
    tags: [verification, formal, verus, stored-command]
    fields:
      method: formal-verification
      steps:
        # Cross-checked against BUILD.bazel by the gate
        # (enforced-by-verus-gate); EXECUTED by verus.yml.
        # The old citation //:verus_relay_sc never existed (#364).
        - run: "bazel test //:relay_sc_verus_test"
    links:
      - type: verifies
        target: SWDD-SC-001
