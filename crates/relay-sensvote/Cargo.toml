[package]
name = "relay-sensvote"
description = "Relay SensVote — redundant-sensor median voting (a faulted sensor never propagates) + GPS-loss INS-fallback freshness monitor"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "plain/src/lib.rs"
crate-type = ["rlib"]

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
