[package]
name = "relay-sec"
description = "Relay Sec — safety-protected comms: anti-replay + authenticated CCSDS frames (designed for reuse across falcon and wohl-class nodes; currently integrated in falcon's C2 link)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "plain/src/lib.rs"
crate-type = ["rlib"]

# no_std + no alloc: portable to M0+ (wohl-class STM32G0) and the falcon Cortex-M7
# alike, and inside the verified flight core. Static buffers only.

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
