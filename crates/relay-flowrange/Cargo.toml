[package]
name = "relay-flowrange"
description = "Relay FlowRange — rangefinder (tilt-compensated terrain altitude) + optical-flow (gyro-compensated velocity) preprocessing for the IEKF"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "plain/src/lib.rs"
crate-type = ["rlib"]

[dependencies]
relay-math = { path = "../relay-math" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
