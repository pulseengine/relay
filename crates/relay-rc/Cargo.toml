[package]
name = "relay-rc"
description = "Relay RC — pilot stick input → manual flight-mode setpoints (Stabilized / Acro), with verified output clamps"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "plain/src/lib.rs"
crate-type = ["rlib"]

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
